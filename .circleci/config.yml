version: 2.1

jobs:
    Test:
        docker:
            - image: piperlincoln/pytest-ubuntu18.04:v0.2
        steps:
            - checkout
            - run: pip install xmldiff
            - run: pip install pytest
            - run: cd project/
            - run: pytest

workflows:
    build:
        jobs:
            - Test

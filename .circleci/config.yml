version: 2.1

jobs:
    Python_2.7:
        docker:
            image: circleci/python:2.7
        steps:
            - checkout
            - run: sudo pip install pytest
            - run: pytest

workflows:
    build:
        jobs:
            - Python_2.7

version: 2.1


executors:
    testing:
        docker:
            - image: pyne/pyne_ubuntu_18.04

jobs:
    testing:
        executor:
            name: testing
        steps:
            - run: pytest

workflows:
    build_and_test:
        jobs:
            - testing

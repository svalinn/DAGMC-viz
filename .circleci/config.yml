version: 2.1

jobs:
    Test:
        docker:
            - image: piperlincoln/pytest-ubuntu18.04
        steps:
            - checkout
            - run: pip install pytest
            - run: pytest

workflows:
    build:
        jobs:
            - Test

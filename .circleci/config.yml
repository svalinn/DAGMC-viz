version: 2.1

jobs:
    Test:
        docker:
            - image: piperlincoln/pytest-ubuntu18.04:v0.1
        steps:
            - checkout
            - run: pip install pytest
            - run: ll /usr/local/visit

workflows:
    build:
        jobs:
            - Test

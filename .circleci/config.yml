version: 2.1

jobs:
    Test:
        docker:
            - image: piperlincoln/pytest-ubuntu18.04:v0.1
        steps:
            - checkout
            - run: pip install pytest
            - run: visit

workflows:
    build:
        jobs:
            - Test
